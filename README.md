# Ане — 31

Поздравительная страница. Один HTML + вынесенные медиафайлы.

- `index.html` — разметка, стили и скрипты (~52 КБ)
- `assets/` — 25 фото, 2 видео, 2 постера

Оптимизировано под мобильные: ленивая загрузка изображений и видео,
тап-таргеты 44px, без `background-attachment:fixed` на тач-устройствах.
Индексация закрыта через `robots.txt` и мета-тег `noindex`.
